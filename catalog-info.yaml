apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: eventcatalog-sample
spec:
  targets:
    - ./groups.yaml
    - ./system.yaml
    - ./inventory-service.yaml
    - ./api-info.yaml
    - ./component-kafka-broker.yaml

